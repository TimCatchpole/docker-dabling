version: '1.0'

services:

  tim-test:
    image: timtest:1.0
    build:
      context: ./
      dockerfile: dockerfile
      args:
        buildversion: 1
        PACKAGES: "wget"
    ports:
      - "3000:80"
#    networks:
#      - app-net
    volumes:
      - ./logs:/var/www/logs
    environment:
      - NODE_ENV=production
      - APP_VERSION=1.0
      - build=4
# networks:
#  app-net:
#    driver: bridge